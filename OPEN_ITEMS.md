# OPEN ITEMS — 미해결 채로 진행하는 것

2라운드 후에도 합의되지 않았거나, 연구자가 미해결을 인지한 채로 진행하기로
결정한 항목. **논문에 그대로 적을 각오가 된 것만 여기 남긴다.**

리뷰어가 물어보기 전에 논문이 먼저 말하는 편이, 리뷰어가 먼저 발견하는 것보다
언제나 낫다.

| # | 항목 | 출처 | 왜 미해결인가 | 논문에서의 처리 |
|---|------|------|--------------|----------------|
| 1 | 원시 아카이브가 수집기 v3의 산출물이며, 그 이전 단계는 존재하지 않는다 | 2026-08-11 Dropbox 전수 확인 | 수집 시점에 KRA가 서빙한 내용과의 대조는 `urls` 표본 재수집으로만 가능 | P0 표본 대조 결과를 데이터 절에 명시 |
| 2 | 수집기가 고급 페이지 키를 만들 때 쓴 두수 N의 출처가 아카이브에 남아 있지 않아, **수집 단계의 마번 누락을 아카이브 내부 검사로 배제할 수 없다** | 2026-08-11 비평 P0 r2-K2, 판정 포기 | 수집기 v3 코드가 이 기기에 없고(전 디렉터리 검색), 아카이브에 남은 것은 페이지 키가 `1..N` 연속이라는 사실뿐이다. N이 Scm 페이지에서 유도됐다면 IC-4의 T와 B가 함께 축소된다. 남은 방어는 IC-4b(페이지 키 개수 vs Scm 마번 열 길이)이며, 이것도 N이 Scm 유도인 경우에는 무력하다 | P0 표본 바이트 대조(SPEC_P0 4.3(a)) 결과로만 판정. 데이터 절에 "아카이브 내부 정합성 검사는 파싱 단계 누락만 배제한다"고 명시 |

| 3 | **`review/RESPONSE_P0_r2.md` K6의 근거에 사실이 아닌 문장이 남아 있다** — "θ는 드물게 쌍승 밖에서도 나온다 — 단승·연승·복승에서 16건, 복연승에서 2건". 실측은 θ가 쌍승 밖 15,381경주(79.7%)에 나타나고 θ 셀의 99.93%가 삼복승·삼쌍승에 있다는 것이다 | 2026-08-11 `analysis/p0_sample.py` 전 페이지 재계수, 원자료 `outputs/p0/strata.csv` | **판정 문서는 연구자만 고친다**(`CLAUDE.md` 금지 1). 게다가 고칠지 자체가 판단 대상이다 — RESPONSE는 그 시점의 판정 기록이므로 사후 수정이 기록의 성격을 바꾼다. 정오표를 붙이는 쪽과 원문을 두는 쪽이 갈린다. **명세서 쪽은 이미 정정했다**(`SPEC_P0.md` 1.2·7·9절), 따라서 현재 두 문서가 어긋나 있다 | 데이터 절에서 θ 분포를 실측치로 적는다. K6 판정 자체(θ가 조합 부재·미출전·파싱 실패의 표식이 아니라는 것)는 기초 페이지 대조에 근거하므로 **이 오류로 무너지지 않는다.** 무너지는 것은 그 판정의 적용 범위가 아카이브 전체라는 인상뿐이다 |

**#3이 왜 라운드 안에서 걸리지 않았나.** `PROCESS.md`가 정한 대로 **비평자는
코드와 원시 데이터를 보지 않는다.** 그래서 문서에 적힌 측정 주장은 비평 루프가
구조적으로 검증할 수 없다 — 비평자가 할 수 있는 것은 진술 간의 모순을 찾는
것뿐이고, 이 문장은 다른 진술과 모순되지 않았다. **측정 주장의 오류는 재측정으로만
잡힌다.** 이번에는 무관한 작업(4.3 층 산출)이 우연히 같은 값을 다시 세면서
드러났다. 우연에 기대지 않으려면 명세서의 측정 주장마다 그것을 낳은 스크립트와
산출 파일을 적어두어야 하며, 1.2절은 이번 정정에서 그렇게 고쳤다.

## θ의 미구분 (부분식별에는 영향 없음)

`9999.9`가 상한 검열인지 무베팅으로 인한 미정의인지는 2026-08-11 측정으로
구분되지 않았다(SPEC_P0 1.2). OPEN_ITEMS에 정식 등재하지 않는 이유는, 두 해석
아래 내재확률 상한 1/θ가 동일해서 `IDEAS.md` I1의 부분식별이 그대로 성립하기
때문이다. **점추정을 시도하는 순간 이 항목은 A형 부채로 승격된다.** 그때
등재한다.

## 프로세스 변경 기록

비평 모델을 바꾸거나 프롬프트를 수정하면 여기에 적는다. 비평의 성격이 달라지므로
이전 라운드와 직접 비교할 수 없다.

| 날짜 | 변경 | 사유 |
|------|------|------|
| 2026-08-11 | `critique.py`가 Responses API를 먼저 시도하고 실패하면 Chat Completions로 폴백 | 최신 모델이 Responses API로 제공되어 단일 경로로는 첫 실행이 죽는다. 실제 사용된 경로는 `CRITIQUE_*.json`의 `api` 필드에 기록된다 |
| 2026-08-11 | `MAX_ROUNDS`를 2에서 4로. 프롬프트의 라운드 문구를 `round_no`로 계산하도록 수정 | 상한이 **문서 집합이 아니라 단계 태그**에 걸려 있어, P0의 두 라운드를 `SPEC_P0.md` 검토에 모두 소진하면 `REPORT_P0.md`를 비평할 라운드가 남지 않는다. r1–r2는 명세서, r3–r4는 리포트에 배정한다. **r1·r2와 r3·r4는 검토 대상 문서가 다르므로 직접 비교할 수 없다.** 상한을 문서 집합 단위로 옮기는 것이 옳은 수정이나 `gate.py`까지 손대야 하므로 보류 |
