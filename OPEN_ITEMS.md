# OPEN ITEMS — 미해결 채로 진행하는 것

2라운드 후에도 합의되지 않았거나, 연구자가 미해결을 인지한 채로 진행하기로
결정한 항목. **논문에 그대로 적을 각오가 된 것만 여기 남긴다.**

리뷰어가 물어보기 전에 논문이 먼저 말하는 편이, 리뷰어가 먼저 발견하는 것보다
언제나 낫다.

| # | 항목 | 출처 | 왜 미해결인가 | 논문에서의 처리 |
|---|------|------|--------------|----------------|
| 1 | 원시 아카이브가 수집기 v3의 산출물이며, 그 이전 단계는 존재하지 않는다 | 2026-08-11 Dropbox 전수 확인 | 수집 시점에 KRA가 서빙한 내용과의 대조는 `urls` 표본 재수집으로만 가능 | P0 표본 대조 결과를 데이터 절에 명시 |
| 2 | 수집기가 고급 페이지 키를 만들 때 쓴 두수 N의 출처가 아카이브에 남아 있지 않아, **수집 단계의 마번 누락을 아카이브 내부 검사로 배제할 수 없다** | 2026-08-11 비평 P0 r2-K2, 판정 포기 | 수집기 v3 코드가 이 기기에 없고(전 디렉터리 검색), 아카이브에 남은 것은 페이지 키가 `1..N` 연속이라는 사실뿐이다. N이 Scm 페이지에서 유도됐다면 IC-4의 T와 B가 함께 축소된다. 남은 방어는 IC-4b(페이지 키 개수 vs Scm 마번 열 길이)이며, 이것도 N이 Scm 유도인 경우에는 무력하다 | P0 표본 바이트 대조(SPEC_P0 4.3(a)) 결과로만 판정. 데이터 절에 "아카이브 내부 정합성 검사는 파싱 단계 누락만 배제한다"고 명시 |

## θ의 미구분 (부분식별에는 영향 없음)

`9999.9`가 상한 검열인지 무베팅으로 인한 미정의인지는 2026-08-11 측정으로
구분되지 않았다(SPEC_P0 1.2). OPEN_ITEMS에 정식 등재하지 않는 이유는, 두 해석
아래 내재확률 상한 1/θ가 동일해서 `IDEAS.md` I1의 부분식별이 그대로 성립하기
때문이다. **점추정을 시도하는 순간 이 항목은 A형 부채로 승격된다.** 그때
등재한다.

## 프로세스 변경 기록

비평 모델을 바꾸거나 프롬프트를 수정하면 여기에 적는다. 비평의 성격이 달라지므로
이전 라운드와 직접 비교할 수 없다.

| 날짜 | 변경 | 사유 |
|------|------|------|
| 2026-08-11 | `critique.py`가 Responses API를 먼저 시도하고 실패하면 Chat Completions로 폴백 | 최신 모델이 Responses API로 제공되어 단일 경로로는 첫 실행이 죽는다. 실제 사용된 경로는 `CRITIQUE_*.json`의 `api` 필드에 기록된다 |
