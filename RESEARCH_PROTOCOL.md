# KRA 배당 상한 복원 연구 프로토콜

## 연구질문

KRA 배당판의 `9999.9` 셀에 실제로 팔린 100원 마권 수
`n ∈ {0,1,2,...}`를 어느 범위까지 식별할 수 있으며, 이 불확실성을 반영해
삼쌍승식 가격정보가 다른 승식의 가격정보를 얼마나 설명하는지 평가한다.

## 관측 구조

경주 `r`, 승식 `m`, 조합 `a`의 마권 수를 `n_rma`, 해당 승식 총 마권 수를
`N_rm`이라 하면

```text
n_rma ∈ {0,1,2,...}
sum_a n_rma = N_rm
O_rma = (1-t_m) S_rm / (100 n_rma),  n_rma > 0
```

이다. `n=0`이면 배당은 큰 유한값이 아니라 정의되지 않는다. 배당판의
`9999.9`는 적어도 다음 두 잠재상태를 구별하지 못한다.

- `n=0`: 무투표 또는 배당 미정의
- `1 <= n <= N_cap`: 실제 배당이 표시 상한을 초과

표시배당은 소수 첫째 자리이므로 미검열 셀도 정확한 `n`이 아니라 반올림과
양립하는 정수구간을 관측한다.

삼쌍승의 모든 유효 순서조합이 19,301경주 전부에서 서로 다른 수치 셀로
완전하게 나타난다는 항등식은 별도 결측표시가 없다는 증거다. 그러나 무투표가
전혀 없는 세계와 무투표가 `9999.9`로 남는 세계를 그 항등식만으로 구별할 수는
없다. 공식 KRA 인코딩 규정을 확보하지 못했으므로 `n=0` 포함은 식별가정으로
명시하고, 총합제약이 모든 양수 모형 또는 모든 0 모형을 기각하는 경주를 별도로
보고한다.

미검열 배당과 상한 셀은 모두 양의 half-up 표시규칙으로 통일한다. 따라서
`9999.9` 관측의 양의 후보는 `참배당 > 9999.9`가 아니라
`참배당 >= 9999.85`인 경우를 포함한다. 더 좁은 참배당 정의는 민감도로만
병기한다.

반올림상 양립하지 않는 미검열 셀이 있는 경주는 두 규칙으로 제시한다.

1. **엄격 규칙**: 셀이 하나라도 불일치하면 경주 전체를 제외한다. 이 표본은
   격자 크기·연도·매출에 따라 선택됨을 표로 보고한다.
2. **셀 완화 규칙**: 불일치 셀만 양의 미지수 `[1,T]`로 강등하고 경주는
   유지한다. 이 규칙의 상한은 제외 경주의 상한 셀 수를 기계적으로 더한 정보 없는
   최악경계이므로 엄격 결과와 동등한 강건성 추정치로 사용하지 않는다.

격자 스냅샷과 매출액이 같은 마감시점의 값이라는 가정은 공식 메타데이터로
확인되지 않았다. `9999.9`가 없는 경주의 자유도 0 회계검사와 전체 불일치
분포를 specification test로 보고한다. 여기에 더해 2016--2019년의 불일치
2,239경주 각각에서 모든 미검열 셀을 동시에 설명하는 하나의 더 이른 총마권 수를
정확히 역산했다. 최종 총마권의 1% 이내와 5% 이내에서 설명 가능한 경주는 모두
0/2,239였다. 따라서 단순한 **가까운 단일 스냅샷 시점 차이**는 이 범위에서
기각하지만, 5%를 넘는 시점차이·다른 표시규칙·당시 데이터 생성 절차 변화까지
배제한 것으로 해석하지 않는다.

승식별 매출은 취소마 관련 환불을 제외한 최종 순매출이라고 가정한다. 무검열
자유도 0 검사는 취소마 유무로 나눠 보고해 이 가정이 관측 회계와 양립하는지 확인한다.

수집 원자료에는 2020·2021년이 없다. 2016--2019년의 반올림 불일치와
2022--2025년의 0건은 데이터 생성 절차의 구조변화를 시사한다. 가까운 단일
스냅샷 가설이 직접검사에서 기각됐으므로 이를 불일치의 설명으로 사용하지 않는다.
따라서 후속 실질 분석은 2022--2025년을 주표본으로 한다. 2016--2019년은
불일치 원인이 해명되지 않은 역사적 진단표본으로만 남기며 강건성 증거로 쓰지 않는다.

`1.0`은 관측 근거 없이 하한 특수코드로 재분류하지 않는다. 일반 한 자리
표시배당으로 회계검사하고, 공식 지급배당과 다르면 미해명 불일치로 남긴다.

## 분석 순서와 현재 상태

1. **완료 — 회계적 부분식별**: 반올림, 정수성, 총판매량만 사용한다.
2. **완료 — 가상 상한 검증**: 2022--2024년으로 배분모형을 선택하고 2025년에
   균등·삼쌍승 내부·다른 승식 보조모형을 시간외 비교한다.
3. **완료 — 희소 다항 기준모형**: 총합이 고정된 잠재 빈도표의 무투표 기대값을
   계산하고 잔여총량 식별구간을 전파한다.
4. **완료 — 과산포 진단**: 실제 0이 없는 가상 상한 선택표본에서 대칭
   Dirichlet--multinomial을 시간외 평가한다. 실제 상한 기대 0은 지원집합 밖의
   기능형식 민감도로만 남기고 주결과로 사용하지 않는다.
5. **완료 — 하위 승식 가격 정합성**: 삼쌍승 완성표를 주변화하고 단승 Harville
   기준선 및 검열 잔여총량 시나리오와 비교한다.
6. **보류 — 구조적 0 확장**: 실제 0이 관측되지 않아 zero-inflated 모형의
   혼합비율을 현재 자료만으로 식별할 수 없다. 외부 정답이나 추가 식별가정 없이는
   수치 추정치를 주결과로 제시하지 않는다.
7. **완료 — 실제 착순 시간외 평가**: 배당판·총매출만으로 먼저 완성한 삼쌍승
   상태분포를 2025년 실제 1·2·3착 순서에 사후 채점한다. 주지표는 다항 Brier,
   비순환 기준선은 단승 Harville, 방향성 반증은 2·3착 축 반전으로 사전 고정한다.

## 정보 분리

- 주분석 복원은 삼쌍승식 내부 정보와 해당 풀 총매출만 사용한다.
- 쌍승·삼복승 등 다른 승식 정보는 복원정확도 극대화 보조모형으로 분리한다.
- 다른 승식으로 복원한 삼쌍승식을 같은 승식의 설명력 평가에 사용해 정보가
  순환하지 않도록 한다.
- 모형 선택·튜닝과 최종 평가는 시간 순서로 분리한다.
- 실제 착순 평가는 각 경주의 확률표가 완성된 뒤에만 `arrival`을 읽는다.
  착순·당첨여부·지급배당은 삼쌍승 상태표 완성에 입력하지 않는다.
- 2025년 착순평가의 모형·주지표·기준선·bootstrap 비교는 결과를 보기 전에
  Draft PR #4에 고정했다.

## 검증

- 미검열 고배당을 인위적으로 가리는 내부 검증
- KRA 상세결과에서 실제 지급액이 공개된 상한 초과 당첨 사례 외부 검증
- 정답이 공개되는 조합은 실제 당첨조합으로 선택된 표본임을 명시하고 전체
  검열 셀의 무작위 표본으로 해석하지 않는다.
- 상세 성적표 HTML 143개와 SHA-256 목록을 동결하고 네트워크 없는 재생성을
  CI에서 수행한다. 지원되는 정수 복원이 후보 0개를 만들면 즉시 실패한다.
- 다섯 배당판의 행·열·고정마 방향은 동결 상세표의 실제 지급항목 전부와
  대조하되, 상한 지급항목은 축 방향을 구별하는 힘이 거의 없으므로 미검열 항목을
  분리 보고한다. 삼쌍승 2·3착 축은 페이지 표제 `(3위 / 2위)`를 주근거로 삼고,
  축을 뒤집은 교차시장 반증모형을 함께 계산한다. `1.0`과 공식 지급배당의 차이도
  실패 통계에서 제외하지 않는다.
- 교차시장 분석의 원시 입력은 별도 preflight로 다시 검사한다. 2022--2025년
  9,671경주·19,827,297개 숫자 행에서 숫자 spanned 셀과 서로 다른 값의 충돌
  중복은 각각 0건이었고, 단승·복승·쌍승·삼복승·삼쌍승의 활성마 조합 support는
  전 경주에서 이론값과 일치했다. 동일값 중복은 2,823,966행이며 값이 같으므로
  dictionary overwrite가 관측가격을 바꾸지 않는다.
- 2016--2019년 반올림 불일치 2,239경주는 모든 미검열 셀을 동시에 만족하는
  더 이른 단일 총마권을 최종값의 1%·5% 이내에서 정확히 탐색하고 0건임을
  `findings/snapshot_timing.md`에 동결한다.
- 2016-07-01 제주 8경주는 8두 출주에서 3착마가 연승식 지급 자격을 갖는다는
  [공식 승식 정의](https://www.data.go.kr/data/15058559/openapi.do)와 달리 상세
  지급표에 해당 항목이 없고 격자는 `9999.9`다. 무투표 해석과 일치하지만 같은
  경주의 다른 연승 격자 두 셀이 최종 지급배당과 불일치하므로 결정적 증명으로
  사용하지 않는다.
- 저장된 선형 착순만으로 발견하지 못할 공동착이 있을 수 있으므로 상한 초과
  지급 144건은 전수 상한이 아니라 확인된 하한이다.
- 가격 정합성 통계는 수집된 경주의 기술적 집계다. 균등배분에서 잔여총량 구간을
  전파하고 비균등 배분 민감도를 병기하되, 경주 간 표본오차의 신뢰구간으로
  해석하지 않는다.
- 실제 착순 평가는 2025년 2,426경주 전부에서 수행하며 제외 0건이다. 삼쌍승
  균등완성과 단승 Harville의 Brier 차이, 2·3착 축 반전과 원래 축의 Brier 차이는
  날짜를 cluster로 2,000회 재표집한 사전 지정 bootstrap 95% 구간을 보고한다.
- 2025년 결과에서 `trifecta_uniform - win_harville` 평균 Brier 차이는
  -0.00098517, 95% 날짜-cluster bootstrap은 [-0.00135358, -0.00058067]이다.
  `swapped_23 - uniform`은 0.00108387, 95% 구간은 [0.00056428, 0.00161204]다.
- 상한 잔여총량 하한·중간·상한의 평균 Brier는
  0.9916293 / 0.9916302 / 0.9916311로 거의 같다. 위치별 비균등
  `beta=0.10`도 주결과와 사실상 동일하다.
- 실현된 삼쌍승 조합 자체가 `9999.9`인 8경주는 모두 사전 회계적 확률하한이
  0이며, 확률상한 중앙값은 0.0000729다. 이 결과는 실현상태를 본 뒤 복원한 것이
  아니라 각 경주의 회계적 식별집합을 사전에 계산한 것이다.
- 착순평가 경주별 16,982행 산출물은 압축을 푼 CSV 내용의 SHA-256
  `c3bc666c1e67f45c3def697bb80b2f4467c37cb97a8d310ef42f5610d202512b`로 고정하고
  CI에서 다시 계산한다.
- 가상 상한 모형은 실제 `n=0`을 포함하지 않으므로 구조적 0의 빈도나 존재를
  검증한 것으로 해석하지 않는다.
- 가상 상한 원표본과 실제 상한 대상의 출전두수·격자크기·셀당 마권 분포를
  함께 보고하며, 겹침 보정 전에는 실제 상한 성능을 검증했다고 표현하지 않는다.

## 주장 경계

내부 가격의 일관성은 실제 착순과의 사후 proper-score 정합성, 객관적 확률의
정확성, 시장효율성, 인과효과, 개인 베터의 행동모수 식별과 서로 다르다.
2025년 착순평가에서 더 좋은 Brier와 log score는 **실현된 상위 세 착순과 가격이
더 잘 정렬된다**는 뜻으로만 해석한다. 시장가격을 확률예측처럼 정규화해 채점한
것이므로 효율성이나 참확률 식별을 직접 주장하지 않는다. 저장된 `arrival`은
선형 순서만 보존해 공동착 구조를 별도로 복원하지 못한다.

## 독립 검토 규칙

- Claude Opus 5(`claude-opus-5`)가 독립적·적대적 학술 검토를 수행한다.
- 한 PR의 성공한 검토는 최대 4회다. 실패한 Actions 실행은 횟수에서 제외한다.
- 각 검토 뒤 연구자가 지적을 판단하고 필요한 변경을 커밋한 다음, 다음 검토를
  `ai-review` 라벨로 명시적으로 요청한다.
- Claude는 파일 수정·커밋·푸시·자동 병합을 하지 않는다.
- 4회 검토를 완료한 뒤 추가 검토가 필요하면 새 PR을 연다.
