# 지금 할 일 — 딱 하나

절단된 배당(9999.9)마다 유한 격자의 상한 K 와 후보 정수 개수 N 을 계산해서
분포를 본다. **네 풀 전부** — 쌍승식(Both), 복연승식(Bc), 삼복승식(3Bc),
삼쌍승식(3Both). 연구의 주 관심사는 삼쌍승식이다.

**고정마 페이지 중복은 없다 (연구자 확인).** 3Bc·3Both 모두 각 고정마
페이지의 조합은 서로 겹치지 않는다. 이전 지시서에서 이걸 이유로 두 풀을
뺐던 건 취소한다 — 모든 `page_variant` 의 셀을 그대로 다 쓰면 된다,
중복 제거 로직은 필요 없다.

**이 작업은 원본 아카이브가 필요 없다.** `데이터/races.jsonl.gz` 와
`데이터/cells/page_key={Both,Bc,3Bc,3Both}/*.csv.gz` 만으로 된다.

**이 파일에서 하는 일은 이것뿐이다.** m 을 데이터로 확정한 뒤 K, N 을
계산하고 분포를 보고한다. 복원(교차 풀 예측)은 하지 않는다 — 다음 단계다.

## 이론 — 정확히 이 공식을 쓴다

공제율 t 는 이미 확정돼 있다: 단승·연승 0.20, 나머지(복승·쌍승·복연승·
삼복승·삼쌍승) 0.27. `(1-t)` 를 기약분수 p/q 로 쓰면(0.20 -> 8/10, 0.27 ->
73/100), 배당 공식은 일반적으로

    O = (1-t)·S / (100·m·n) = p·S / (100·q·m·n)

이다. n 은 그 조합에 걸린 100원 단위마권 수(정수 >= 1), m 은 풀마다 다른
구조적 배수다 (복연승식은 m=3 으로 이미 확정돼 있다 — 과거 `p0_k_probe.py`).
쌍승식·복승식은 m=1. 삼복승식·삼쌍승식의 m 은 **추측하지 않고 아래에서
데이터로 정한다**.

절단 표시 c = 9999.9 는 상한이므로, 절단 셀의 참배당 O* 는 c < O* <= K 를
만족한다. K = (1-t)·S/(100·m) 이고 `fractions.Fraction` 으로 정확히 계산한다
(부동소수점 금지 — 격자 경계가 0.1 하나로 갈릴 수 있다).

    n < K/c   =>   후보는 n ∈ {1, ..., N},   N = ceil(K/c) - 1

N=1 이면 O* = K 로 점식별된다.

## 절차

0. `git pull --ff-only` 로 최신을 받는다.

1. `데이터/races.jsonl.gz` 를 읽어 race_id -> {라벨: 금액(원)} 맵을 만든다
   (쉼표·"원" 제거하고 정수로).

2. **삼복승식·삼쌍승식의 m 을 확정한다.** 절단 안 된 셀(`cell_raw` 가
   "9999.9" 도 "1.0" 도 아닌 것) 을 표본 경주 10~20개(출주두수가 8~16두로
   다양하게)에서 모아, 후보 m ∈ {1, 2, 3, 4, 6} 각각에 대해 정수 n 이
   존재해서

       표시값 d = round(10 · p·S / (100·q·m·n))

   이 관측된 표시값과 일치하는지 검사한다. 어떤 m 이 셀 거의 전부에서
   일관되게 재현되는지가 답이다. 재현율이 가장 높은 m 을 채택하고, 그
   m 이 얼마나 깨끗하게 이기는지(재현율 %) 를 기록한다 — 애매하면(두 m 이
   비슷하게 잘 맞으면) 계산을 멈추고 그 사실을 그대로 보고한다.

3. 확정된 m 으로, 네 풀(Both, Bc, 3Bc, 3Both) 전부에서 `cell_raw` 가 정확히
   `"9999.9"` 인 셀을 찾는다. 마번 열은 제외한다. 3Bc/3Both 는 모든
   `page_variant` 의 셀을 그대로 합친다(중복 없으니 그대로 합치면 된다).

4. 절단 셀마다 race_id 로 해당 풀의 S 를 찾아(못 찾으면 "S 없음" 으로 따로
   센다) K, N 을 계산한다.

5. 참고용으로 단승식·연승식·복승식(Scm)의 9999.9 개수만 센다(계산 없이).

## 보고할 것

- 삼복승식·삼쌍승식 m 확정 결과: 채택된 m, 재현율, 후보 간 비교표
- 풀(Both/Bc/3Bc/3Both) 별로:
  - 절단 셀 총 개수, 그 절단 셀을 가진 경주 수 / 전체 경주 수
  - N 의 분포: 최소·1사분위·중앙값·3사분위·최대, N=1(점식별) 개수와 비율,
    N<=5, N<=10, N<=50 각각의 개수와 비율
  - K 의 분포 참고용 (최소·중앙값·최대, 원 단위)
  - "S 없음" 으로 건너뛴 셀이 있으면 개수와 예시 race_id
- Scm 의 9999.9 개수 (참고용)
- **삼쌍승식에 특히 집중해서**: N=1 비율이 얼마나 되는지, 이게 P2 아이디어
  (부분식별)가 이 풀에서 실질적으로 쓸 만한지 가늠할 첫 숫자라는 점을
  결론에 한 문장으로 적는다

## 결과를 남기는 방법 — 반드시 이렇게

1. `findings/censored_grid.md` 파일을 만든다.
2. `git add findings/censored_grid.md && git commit -m "절단 배당 K/N 분포 실측 (4개 풀)" && git push`
   — push 가 거부되면 `git pull --rebase` 후 다시 `git push`.
3. `git log -1` 로 확인하고 커밋 해시를 마지막 줄에 출력한다.

## 하지 않는 것

- 교차 풀 예측(단승 배당으로 절단 셀 추정)은 하지 않는다 — 다음 단계다
- 고정마 페이지 중복 제거 로직을 만들지 않는다 — 중복이 없다고 이미
  확인됐으므로 불필요하다
- `kra/*.py`, `parse_archive.py` 를 고치지 않는다
- `findings/censored_grid.md` 외에 다른 파일을 만들지 않는다
- P0/P1/P2 같은 이름을 쓰지 않는다
