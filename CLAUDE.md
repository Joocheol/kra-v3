# 지금 할 일 — 딱 하나

절단된 배당(9999.9)마다 유한 격자의 상한 K 와 후보 정수 개수 N 을 계산해서
분포를 본다. 이게 P2 아이디어("쌍승식 절단 배당의 부분식별")의 첫 실측이다.

**이 작업은 원본 아카이브가 필요 없다.** `데이터/races.jsonl.gz` (매출액,
검증 완료 — `findings/sales_verification.md`) 와 `데이터/cells/`
(셀 단위 배당률, 검증 완료 — `findings/cells_committed.md`) 만으로 된다.
`--archive` 플래그도, Dropbox 경로도 필요 없다. 이 지시서를 읽는 사람이
로컬이든 클라우드든 레포만 체크아웃하면 돌아간다.

**이 파일에서 하는 일은 이것뿐이다.** K, N 을 계산하고 분포를 보고한다.
복원(교차 풀 예측, Harville 류 구조모형)은 하지 않는다 — 그건 다음 단계다.

## 이론 — 정확히 이 공식을 쓴다

풀별 공제율 t 는 이미 확정돼 있다: 단승·연승 0.20, 복승·쌍승·복연승·삼복승·
삼쌍승 0.27. `(1-t)` 를 기약분수 p/q 로 쓰면(0.20 -> 8/10, 0.27 -> 73/100),
발매금액 S(원)에 대해 그 풀의 최대 배당(상한) K 는

    K = (1-t)·S / 100 = p·S / (100·q)

이다. 실수지만 유리수이므로 `fractions.Fraction` 으로 정확히 다룬다(부동소수점
반올림을 쓰지 않는다 — 격자 경계가 0.1 하나로 갈릴 수 있다).

절단 표시 c = 9999.9 는 "표시값이 9999.9 이상이면 그대로 자른다"는 상한이므로,
절단된 셀의 참배당 O* 는 c < O* <= K 를 만족한다. O* = K/n (n = 그 조합에
걸린 100원 단위마권 수, 정수 >= 1) 이므로

    n < K/c   =>   후보는 n ∈ {1, 2, ..., N},   N = ceil(K/c) - 1

N=1 이면 후보가 n=1 하나뿐이라 O* = K 로 점식별된다. N 이 작을수록 격자가
좁아 사실상 점식별에 가깝다.

## 대상 풀 — 쌍승식(Both), 복연승식(Bc) 만

**삼복승식(3Bc), 삼쌍승식(3Both)은 이번에 뺀다.** 두 페이지는 고정마별로
여러 장 나뉘어 있어 같은 조합(예: 7-5-1)이 페이지마다 반복 등장한다. 이걸
셀 단위로 그대로 세면 같은 절단 셀을 여러 번 세게 되므로, 중복 제거 규칙을
먼저 확정해야 한다 — 그건 별도 검증 항목이다(과거 `p0_k_probe.py` 도 같은
이유로 이 두 풀을 C 단계에서 뺐다). 단승식·연승식·복승식은 조합 수가
출주두수만큼(<=16)뿐이라 절단이 사실상 없을 것으로 예상되는데, 그 예상이
맞는지만 참고로 확인한다(절단 셀 개수만 세고 자세한 분석은 안 함).

## 절차

0. `git pull --ff-only` 로 최신을 받는다.

1. `데이터/races.jsonl.gz` 를 읽어 race_id -> sales(딕셔너리) 맵을 만든다.
   `sales` 의 라벨(예: "쌍승식")에서 금액 문자열(예: "289,737,000원")을 정수
   원으로 바꾼다(쉼표·"원" 제거).

2. `데이터/cells/page_key=Both/*.csv.gz` 와 `page_key=Bc/*.csv.gz` 를
   순회한다. `section == "body"`, `col_header` 가 마번 열이 아닌 셀만 본다
   (`col_group` 이나 헤더 구조는 `kra/race.py` 의 `_horse_column` 로직을
   참고). `cell_raw` 가 정확히 `"9999.9"` 인 셀이 절단 셀이다.

3. 절단 셀마다: race_id 로 해당 풀의 S 를 찾고(못 찾으면 그 셀은 건너뛰고
   "S 없음" 으로 따로 센다), 위 공식으로 K, N 을 계산한다.

4. 참고용으로 단승식·연승식·복승식(Scm 페이지)에서도 `cell_raw == "9999.9"`
   개수만 센다 (K, N 계산은 안 함).

## 보고할 것

풀(쌍승식/복연승식)별로:

- 절단 셀 총 개수, 그 절단 셀을 가진 경주 수 / 전체 경주 수
- N 의 분포: 최소·1사분위·중앙값·3사분위·최대, 그리고 N=1(점식별) 개수와
  비율, N<=5, N<=10, N<=50 각각의 개수와 비율
- K 의 분포도 참고로 (최소·중앙값·최대, 원 단위)
- "S 없음" 으로 건너뛴 셀이 있으면 몇 개인지와 예시 race_id 하나

그리고:

- Scm 페이지(단승·연승·복승)의 9999.9 개수 — 예상대로 0에 가까운지

## 결과를 남기는 방법 — 반드시 이렇게

1. `findings/censored_grid.md` 파일을 만든다.
2. `git add findings/censored_grid.md && git commit -m "절단 배당 K/N 분포 실측" && git push`
   — push 가 거부되면(다른 작업이 동시에 main 에 쓰고 있을 수 있다)
   `git pull --rebase` 후 다시 `git push`.
3. `git log -1` 로 확인하고 커밋 해시를 마지막 줄에 출력한다.

## 하지 않는 것

- 삼복승식·삼쌍승식은 분석하지 않는다 (중복 제거 규칙 미확정)
- 교차 풀 예측(단승 배당으로 절단 셀 추정)은 하지 않는다 — 다음 단계다
- `kra/*.py`, `parse_archive.py` 를 고치지 않는다
- `findings/censored_grid.md` 외에 다른 파일을 만들지 않는다
- P0/P1/P2 같은 이름을 쓰지 않는다
