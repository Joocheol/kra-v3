# 지금 할 일 — 딱 하나

`kra/race.py`의 `_sales_and_notice`가 Scm 페이지 `<tfoot>`에서 매출액을
넷만 읽어 온다 (단승·연승·복승·"총매출액"으로 추정되는 라벨). 그런데 화면
캡처에는 일곱 승식 전부의 매출액과 "합 계"가 있다:

    단승식 25,650,700   연승식 36,571,800   복승식 491,945,100
    쌍승식 174,175,100  복연승식 77,140,900  삼복승식 709,248,600
    삼쌍승식 488,040,700  합 계 2,002,772,900

**이 파일에서 하는 일은 이것뿐이다.** 셋이 왜 사라지는지 원자료로 확인하고
보고한다. 파서 수정, 다른 개선, 다음 단계 제안은 하지 않는다. 확인이 끝나면
멈추고 결과를 보고한다.

## 절차

1. 아카이브에서 임의로 한 경주를 고른다.
   `~/Dropbox/kra-analysis/data/raw_collected_v3_15w/kra_*/raw_archive/*/*/*.json.gz`

2. `gzip.decompress` 로 연 뒤, **파서를 거치지 않고** `payload["pages"]["Scm"]`
   의 원문 HTML에서 정규식으로 `<tfoot>...</tfoot>` 를 그대로 뽑아 출력한다.
   문자열이 `dict` 로 온 경우(고정마 변형)와 `str` 로 온 경우를 둘 다 다룬다.

3. 그 원문 안에서 다음을 눈으로 센다.
   - 콜론(`:`)을 포함한 셀이 몇 개인가, 각각의 라벨과 값
   - "합", "총", "쌍승", "복연승", "삼복승", "삼쌍승" 이라는 문자열이 각각
     tfoot 안에 몇 번 나오는가
   - tfoot이 몇 개의 `<tr>` 로 이루어져 있는가, rowspan/colspan 이 있는가

4. `kra/htmltable.py` 의 `parse_tables` 로 같은 HTML을 파싱해서
   `table.section("foot")` 의 셀 목록을 뽑고, 3번의 원문 결과와 나란히 놓는다.
   `_sales_and_notice`(kra/race.py)가 정확히 어느 셀을 왜 놓치는지 짚는다 —
   예: spanned 처리, 콜론 파싱 위치, 라벨 텍스트의 공백/개행.

5. 서로 다른 경주 5~10개로 반복해서, 넷만 잡히는 게 항상 그런지 아니면
   경주마다 다른지 확인한다.

## 보고 형식

- tfoot 원문 예시 1개 (그대로, 축약 없이)
- 원문에 실제로 몇 개의 매출액이 있는지
- `_sales_and_notice` 가 놓치는 정확한 지점 (코드 줄 단위로)
- 5~10개 경주에서 패턴이 일관됐는지

## 하지 않는 것

- `kra/race.py` 를 고치지 않는다
- 다른 파일을 만들지 않는다
- "다음으로 이런 것도 확인하면 좋겠다" 는 제안을 하지 않는다
- P0/P1/P2 같은 이름을 쓰지 않는다
