# 지금 할 일 — 딱 하나

**연구의 주 관심사는 삼쌍승식(3Both)이다.** 직전 지시서에서 "고정마 페이지
중복 문제 미확정"을 이유로 삼복승·삼쌍승을 뺐는데, 그건 빼도 되는 근거가
아니라 먼저 풀어야 할 문제였다. 이번 파일은 그 문제를 실제로 푼다 — 절단
배당의 K, N 계산(다음 단계)은 이 진단이 끝난 뒤에 한다.

**이 작업은 원본 아카이브가 필요 없다.** `데이터/races.jsonl.gz` 와
`데이터/cells/page_key=3Bc/*.csv.gz`, `page_key=3Both/*.csv.gz` 만으로
된다. `CellRow` 에는 `page_variant`(고정마 번호) 열이 이미 있다.

**이 파일에서 하는 일은 이것뿐이다.** 두 가지를 실측하고 보고한다. 계산이나
복원은 하지 않는다.

## 배경 — 왜 확인이 필요한가

`kra/race.py` 문서: "3Bc 삼복승 unordered triple, one page per fixed horse"
"3Both 삼쌍승 ordered triple, header "( 3위 / 2위 )", one page per fixed horse"

두 풀은 구조가 다를 수 있다.

- **3Bc(순서 없음)**: {A,B,C} 조합이 A·B·C 각각의 고정마 페이지에 전부
  나타날 수 있다 (진짜 중복 가능성).
- **3Both(순서 있음)**: 캡션이 "(3위/2위)"인 것으로 보아, 고정마가 1착을
  가리키고 그 페이지의 표가 2착·3착 조합을 나타낼 가능성이 있다. 맞다면
  "A 1착-B 2착-C 3착"은 A 의 페이지에만 나오고 중복이 없다 — 순서가 고정마로
  이미 정해지기 때문이다.

이건 추측이다. 확인 없이 다음 단계로 넘어가면 안 된다. 셀 데이터가 이미
있으니 실제로 세어 보면 된다.

## 확인 1 — 중복 구조

경주마다:

- `n` = 그 경주의 출주두수(취소 제외, `데이터/races.jsonl.gz` 의
  `n_registered`, `scratched` 로 계산 — `n_registered - len(scratched)`)
- 3Both: 그 경주의 **모든** 고정마 페이지(`page_variant` 전부)를 합쳐, 유효
  셀(스팬되지 않은 셀, 마번 열 제외, 대각선/구조적 빈칸 제외)의 총 개수를
  센다. 순서 있는 트리오의 전체 개수는 `n·(n-1)·(n-2)` 다. 총 개수를 이
  값과 비교한다 — 같으면 중복 없음, 대략 3배면 셋 중 하나가 앞자리에
  걸리는 식의 중복, 다른 배수면 그 배수를 그대로 보고한다.
- 3Bc: 같은 방식으로, 순서 없는 트리오 전체 개수는 `C(n,3) = n(n-1)(n-2)/6`
  이다. 총 개수를 이 값과 비교한다.

경주 10~20개를 표본으로 골라(출주두수가 다양하게 섞이도록, 예를 들어 8두·
12두·16두 각각 몇 개씩) 이 비율이 경주마다 일관되는지 확인한다. 일관되면
그 배수가 곧 중복 계수다.

추가로: 3Both 에서 같은 (row_header, col_header) 짝이 서로 다른
`page_variant` 에 걸쳐 **같은 값**으로 나타나는지, 아니면 `page_variant`
가 달라지면 그 조합 자체가 다른 순서(다른 물리적 결과)를 가리키는지도
직접 몇 개 사례를 뽑아 눈으로 확인해 적는다 — 이게 위 개수 비교의 해석을
결정한다.

## 확인 2 — 배당 공식의 배수 m

복연승식(Bc)은 이미 m=3 으로 확정돼 있다(과거 `p0_k_probe.py`). 삼복승·
삼쌍승도 비슷한 배수가 있을 수 있는데, 추측하지 않고 데이터로 확정한다.

공제율은 둘 다 0.27 (p=73, q=100)이다. 절단되지 않은 셀(`cell_raw` 가
`"9999.9"` 도 `"1.0"` 도 아닌 것)에 대해, 후보 정수 n 과 후보 배수
m ∈ {1, 2, 3, 4, 6}(확인 1 에서 나온 중복 계수가 있으면 그것도 후보에
넣는다) 을 각각 시도해서

    표시값 d = round(10 · p·S / (100·q·m·n))

가 관측된 표시값과 일치하는 n 이 존재하는지 본다. 어떤 m 이 셀 대부분에서
일관되게 재현되는지가 답이다. 표본 경주 5~10개, 절단 안 된 셀 전체로
확인한다.

## 보고할 것

- 3Both: 경주별 유효 셀 총수 / n(n-1)(n-2) 비율 — 표본 전체에서 일관된
  값인지, 값이 얼마인지
- 3Both: (row_header, col_header) 같고 page_variant 다른 사례 2~3개를
  구체적으로 — 값이 같은지 다른지, 다르다면 그게 왜 다른 물리적 결과인지
- 3Bc: 경주별 유효 셀 총수 / C(n,3) 비율 — 같은 방식으로
- 두 풀 각각의 m 확정 결과 — 어떤 m 이 재현하는지, 재현 못 하는 셀이
  있다면 몇 %인지
- 결론: 3Both, 3Bc 각각에 대해 "중복 없음 / 배수 X 로 중복 — 이런 규칙으로
  제거하면 된다" 를 명확한 한 문장으로

## 결과를 남기는 방법 — 반드시 이렇게

1. `findings/triple_pool_structure.md` 파일을 만든다.
2. `git add findings/triple_pool_structure.md && git commit -m "삼복승·삼쌍승 구조 실측" && git push`
   — push 가 거부되면 `git pull --rebase` 후 다시 `git push`.
3. `git log -1` 로 확인하고 커밋 해시를 마지막 줄에 출력한다.

## 하지 않는 것

- 절단 셀의 K, N 계산은 하지 않는다 — 이 진단이 끝난 뒤 다음 단계다
- 쌍승식·복연승식은 다시 보지 않는다 (이미 끝났다 — `findings/censored_grid.md`
  가 아직 없다면 그건 이 작업 다음에 한다)
- `kra/*.py`, `parse_archive.py` 를 고치지 않는다
- `findings/triple_pool_structure.md` 외에 다른 파일을 만들지 않는다
- P0/P1/P2 같은 이름을 쓰지 않는다
