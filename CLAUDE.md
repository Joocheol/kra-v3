# 지금 할 일 — 딱 하나

`kra/race.py`의 `_sales_and_notice`/`parse_race`를 고쳐서 (커밋 2a6769a)
Scm 한 페이지가 아니라 다섯 페이지(Scm/Both/Bc/3Bc/3Both) 전부에서 매출액을
모으도록 했다. `findings/tfoot_diagnosis.md`에 있는 경주 하나로만 손으로
검증했고, 아카이브 전체로는 아직 확인하지 않았다.

**이 파일에서 하는 일은 이것뿐이다.** 고친 파서를 아카이브에 돌려 세 가지를
확인하고 보고한다. 코드는 고치지 않는다. 확인이 끝나면 멈추고 결과를
보고한다.

## 확인할 것 셋

1. **일곱 승식이 항상 다 잡히는가.** `race.sales` 에 단승·연승·복승·쌍승·
   복연승·삼복승·삼쌍승 일곱 라벨이 (그리고 Scm 페이지 자체의 "총매출액"
   라벨까지 여덟이) 매 경주 다 있는가, 아니면 일부 경주에서 빠지는 라벨이
   있는가.

2. **일곱 승식의 합이 총매출액과 항상 맞는가.** 단승+연승+복승+쌍승+복연승+
   삼복승+삼쌍승 == 총매출액 (원 단위까지) 인지 경주마다 확인한다. 어긋나면
   그 경주의 `race.sales` 전체와 race_id 를 따로 적어 둔다.

3. **`race.problems` 에 "매출액 불일치" 가 몇 건 잡히는가.** 이건 3Bc/3Both
   고정마 변형끼리 같은 총액이 반복되지 않은 경우다. 0건이 이상적이다. 있다면
   그 race_id 와 어떤 라벨이 어떤 값으로 갈렸는지 적는다.

## 절차

0. `git pull --ff-only` 로 최신을 받는다.

1. 아카이브 전체(`~/Dropbox/kra-analysis/data/raw_collected_v3_15w`)를
   `kra.archive.months` 로 순회하며 `kra.race.parse_race` 를 돌린다. 표본이
   아니라 전량으로 한다 — 19,301 경주는 이 정도 파싱에 몇 분이면 끝난다.

2. 경주마다 위 세 가지를 판정해 집계한다. 판정 기준:
   - "라벨 누락": 여덟 라벨 중 하나라도 `race.sales` 에 없음
   - "합계 불일치": 일곱 승식 합 != 총매출액 (문자열에서 쉼표/"원" 제거하고
     정수로 비교)
   - "고정마 불일치": `race.problems` 안의 "매출액 불일치" 문자열 개수

3. 세 판정 모두 0건이면 그것도 명확히 "0건" 이라고 적는다. 있으면 해당
   경주의 race_id, 원인으로 보이는 값, 개수를 표로 남긴다.

## 결과를 남기는 방법 — 반드시 이렇게

1. `findings/sales_verification.md` 파일을 만든다.
2. 전체 경주 수, 세 판정의 건수, (있다면) 문제 경주 목록을 담는다.
3. `git add findings/sales_verification.md && git commit -m "매출액 수집 수정 검증" && git push`
   로 `main` 에 직접 올린다.
4. `git log -1` 로 커밋을 확인하고 해시를 마지막 줄에 출력한다.

## 하지 않는 것

- `kra/race.py` 를 고치지 않는다 (문제를 발견해도 고치지 말고 보고만 한다)
- `findings/sales_verification.md` 외에 다른 파일을 만들지 않는다
- "다음으로 이런 것도 확인하면 좋겠다" 는 제안을 하지 않는다
- P0/P1/P2 같은 이름을 쓰지 않는다
