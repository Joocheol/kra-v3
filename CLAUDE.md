# CLAUDE.md — 이 레포에서 지킬 것

이 프로젝트는 **논문을 쓰는 중이 아니다.** KRA 배당 데이터에서 논문이 될
아이디어를 찾는 동시에, **그 과정이 어디까지 자동화되는지 시험한다.**
자세한 것은 `PROCESS.md` 0절.

그 메타 목적 때문에 아래 규칙은 편의가 아니라 **관찰 대상**이다. 규칙을 어기면
시험이 무의미해진다. 어겨야 할 이유가 생기면 어기지 말고 먼저 말하라.

---

## 절대 하지 않는 것

**1. `review/RESPONSE_*.md`를 쓰지 않는다.**
비평에 대한 판정(반박·수정·포기·백로그)은 **연구자만** 한다. 두 모델이 합의해서
함께 틀리는 것이 이 구조가 막으려는 최악의 결말이다. 판정 초안을 요청받으면
초안임을 명시하고 대화에 제시하되, 파일로 커밋하는 것은 연구자의 확인 뒤에 한다.

**2. 게이트를 우회하지 않는다.**
`python3 scripts/gate.py --phase P0`이 닫혀 있으면 다음 단계 작업을 시작하지
않는다. 한 단계가 닫히기 전에 다음 것을 시작하면 그 순간 시험 대상을 부순 것이다.

**3. `.github/` 아래를 수정하지 않는다.**
권한 밖이다(`AUTOMATION_LOG.md` A-02). 변경이 필요하면 내용을 제시하고 연구자가
커밋한다.

**4. 원시 데이터를 수정하지 않는다.**
`~/Dropbox/kra-analysis/data/raw_collected_v3_15w/` 이하는 **읽기 전용**이다.
재수집분도 아카이브에 덮어쓰지 않고 별도 경로에 둔다.

**5. 경로를 지어내지 않는다.**
`pwd`와 실제 목록으로 확인한 뒤 쓴다. 기억이나 추측으로 경로를 만들지 않는다.

**6. `p0-spec`에 직접 푸시하지 않는다.**
작업은 작업 브랜치에서 한다. 병합은 연구자가 지시할 때만, squash로. 아래
작업 습관에 이유가 있다.

**7. 뒤처진 상태에서 작업을 시작하지 않는다.**
시작 전에 `git pull`. 로컬 HEAD가 `origin/p0-spec`보다 뒤처져 있으면 시작하지
않는다. 2026-08-11에 이것을 어겨 `CLAUDE.md`가 두 번 만들어졌다(A-08).

---

## 실행 순서 — 이 프로젝트에서 가장 자주 깨지는 것

`analysis/SPEC_P0.md`는 **특정 파일이 특정 실행보다 먼저 커밋될 것**을 요구한다.
이것은 형식이 아니라 비평 답변의 근거 자체다. 순서가 뒤바뀌면
`review/RESPONSE_P0_r2.md`가 거짓이 된다.

| 먼저 커밋 | 그 다음에야 실행 |
|---|---|
| `outputs/p0/normalize_rules.md` | 재수집 |
| `outputs/p0/sample_38.json` | 재수집 |
| `outputs/p0/transcript/` | 파서 결과와의 비교 |

**`survey.json` 앞의 순서 요구는 없어졌다 (2026-08-11).** 5.1의 임계가 0으로
확정되면서 "분포에서 임계로 가는 결정 규칙"이 삭제됐고, 고정할 규칙이 없으므로
그것을 먼저 커밋했는지 증명할 일도 없다. `--survey`는 남지만 목적이 파싱 전
전수 조사로 바뀌었다(SPEC_P0 6절).

**전사(4.3b)는 파싱보다 시간상 앞서면 가장 깨끗하다.** 전사 담당자가 파싱 결과에
노출되면 안 된다. 커밋 타임스탬프가 순서의 증거다.

순서를 지킬 수 없는 상황이면 **실행하지 말고 보고하라.** 사후에 커밋해서
순서를 맞춘 것처럼 보이게 하는 것이 가장 나쁘다.

---

## 검정을 설계할 때

`CONVENTIONS.md` §1의 원칙이다. 이 프로젝트가 두 번 어겼고 두 번 다 비평에
걸렸다(r1-K1/K5, r2-K2).

> **검정 기준 B를 검정 대상 T와 같은 경로에서 만들지 않는다.**

새 검사를 추가할 때 T·B·실패 조건을 **표에 명시적 칸으로** 적는다. 산문으로
쓰면 빠뜨린다. B의 계산에 T나 T를 만든 규칙이 들어가면 순환이며 삭제 대상이다.

파생 필드를 만들면 `outputs/p0/lineage.csv`에 행을 추가한다.
**계보표에 없는 필드는 산출물에 존재할 수 없다.** `warrant`가 `없음`인 파생
필드는 동결 조건 위반이므로, 그 상태로 남길 거면 명시적으로 보고하라.

---

## 파서 층 분리

| 층 | 하는 일 |
|---|---|
| L1 `raw_cells/` | HTML → 셀. **원문 그대로.** 판정 없음 |
| L2 `derived/` | 도착순서 + 규정 → 적중, 지급 착순 수 (규칙은 SPEC_P0b) |
| L3 `diagnostics/` | 검열, 이상 토큰, 정합성 위반 |

L1에 들어가서는 안 되는 필드: `is_hit`, `place_cutoff`, `is_capped`,
`is_valid`, `is_cancel`. **파서는 판정하지 않는다.** v1·v4의 실패 8건 중
대부분이 파서가 규칙으로 계산해낸 파생 필드였다.

숫자 변환은 L3에서만 한다. 정규식에 **정확히** 매칭될 때만 float으로 바꾸고,
아니면 `None` + `cell_raw` 보존 + `cell_class=unknown_token`. `re.search`로
숫자를 뽑아내는 느슨한 폴백은 v4의 실패이며 반복하지 않는다.

---

## 막히면 기록한다

`PROCESS.md` 0절은 **자동화가 막힌 지점이 이 프로젝트의 일차 산출물**이라고
선언한다. 그 기록은 `AUTOMATION_LOG.md`에 쌓는다.

사람이 개입해야만 했던 곳, 도구가 못 한 것, 설계가 틀려서 우회한 것이 생기면
**그 자리에서 등재한다.** 한 항목에 셋을 적는다 — 무엇이 막혔나 / 사람이 무엇을
대신했나 / 고칠 수 있는가. 등급은 구조·설계·운용·권한.

나중에 회상해서 쓰면 이미 절반은 잃은 뒤다.

---

## 작업 습관

### 브랜치 — 작업은 작업 브랜치에서 한다

`p0-spec`에서 갈라낸 **작업 브랜치**에서 일한다. 이름은 작업 내용으로 짓는다 —
`p0-sample`, `p0-parser`처럼.

작업 브랜치에는 **허락을 묻지 않고** 커밋하고 푸시한다. 작업 단위가 끝날 때마다
끊어서 커밋한다.

**푸시하면 알린다** — 브랜치명과 커밋 해시를 함께. 연구자가 `git pull`을 해야
하고, 어느 브랜치를 당길지 알아야 한다.

**`p0-spec` 병합은 연구자가 지시할 때만, squash로 한다.** 푸시된 커밋
타임스탬프는 되돌릴 수 없고, 위 실행 순서 표가 요구하는 순서의 유일한 증거가
그 타임스탬프다. 작업 브랜치는 시행착오가 남아도 된다. 그러나 `p0-spec`에
**"같은 산출물을 두 번 만들었다"가 남으면** 결과를 보고 다시 만든 것 아니냐는
의심을 부르고, 그 질문에는 답할 방법이 없다. squash 병합이면 `p0-spec`에는
확정 시점 하나만 남는다.

### 시작 전에 `git pull` 한다 — 예외 없음

**작업을 시작하기 전에 항상 `git pull`로 `p0-spec`을 최신으로 맞춘다.** 작업
브랜치를 새로 만들 때도, 기존 작업 브랜치로 돌아올 때도 마찬가지다.

**로컬 HEAD가 `origin/p0-spec`보다 뒤처져 있으면 작업을 시작하지 않는다.**

레포에 쓰는 손이 여럿일 때 뒤처진 상태에서 시작하면 **같은 파일이 양쪽에서
따로 만들어진다.** 2026-08-11에 `CLAUDE.md`가 두 번 만들어졌다
(`AUTOMATION_LOG.md` A-08, A-06의 재발).

### 그 밖에

**측정은 스크립트로 남긴다.** 즉석 계산하고 결과만 보고하지 않는다. 재현
가능해야 하고, 나중에 비평자가 숫자의 출처를 물을 때 파일을 가리킬 수 있어야
한다. `analysis/p0_probe.py`가 그 형태다.

**커밋 메시지에 왜를 적는다.** 무엇을 바꿨는지는 diff가 말한다. 어느 비평
항목에 대응하는지, 어떤 대안을 기각했는지를 적는다.

**범위 밖 아이디어는 실행하지 않고 적는다.** 연구 질문이면 `IDEAS.md`, 부수
작업이면 `BACKLOG.md`. 지금 실행하지 않고 현재 단계를 닫는다.

**한국어로 답한다.** 코드 주석은 영어여도 된다.

---

## 지금 상태

- 단계 브랜치 `p0-spec`, PR #1 열림. 작업 브랜치 `p0-sample`
- P0 게이트 **열림**. `SPEC_P0.md`가 비평 2라운드를 통과했다
- `analysis/p0_sample.py` 작성됨. `outputs/p0/strata.csv` 산출(19,301 전수)
- **`sample_38.json` 확정.** 표본 크기는 연구자 판정으로 40 → **38**. 36칸
  교차 중 2칸이 비어(2025년 서울·부경에 등록두수 ≤7 경주 0건) 34칸 × 1 +
  커버리지 4 = 38. 빈 칸은 정원 0으로 산출물에 남겼다. 중단 기록은
  `outputs/p0/sample_stop_v1.log`
- 비평 라운드 배정: r1–r2는 `SPEC_P0.md`(사용함), r3–r4는 `REPORT_P0.md`

참조: `PROCESS.md` 절차 · `analysis/SPEC_P0.md` 이번 단계 명세 ·
`review/RESPONSE_P0_r2.md` 최근 판정 · `OPEN_ITEMS.md` 미해결 ·
`AUTOMATION_LOG.md` 막힌 지점
