# BACKLOG — B형 항목

현재 주장 대장의 어떤 ID도 위협하지 않지만, 언젠가 할 가치가 있는 것들.
여기 들어온 항목은 **이번 논문에서 다루지 않는다.** 예외 없음.

형식:
- [ ] #1 (출처: CRITIQUE_P1_r1/K3) 한 줄 설명 — 왜 지금이 아닌가

---

- [ ] #1 (출처: 2026-08-11 P0 측정 중 파생) 표시 상한에서 잘린 배당의 부분식별 —
      θ 뒤의 값을 몰라도 "데이터가 맞다"는 P0의 주장은 무너지지 않으므로 B형이다.
      아래 #1 상세 참조.

- [ ] #2 (출처: 2026-08-11 성적표 실물 확인) 경주성적표를 이용한 적중 조합 배당 대조 —
      성적표(`ScoretableDetailList.do`)에 적중 조합의 배당률과 풀별 매출액이 실려 있다.
      배당표와 다른 페이지이므로 파서 독립 대조 경로가 될 수 있으나, SPEC_P0 4.3(b)의
      사람 전사 검증과 역할이 겹친다. 배치를 다시 설계해야 하며 P0의 주장을 위협하지 않는다.

---

## #1 상세 — 표시 상한에서 잘린 배당의 부분식별

### 무엇을 알고 싶은가

쌍승 배당표에서 `9999.9`로 인쇄된 셀의 실제 배당률.
2026-08-11 측정으로 이것이 관측 실패가 아니라 **표시 상한**임이 확정됐다
(θ가 걸린 조합 187건 전부에서 같은 두 마리의 복연승 배당이 정상 존재.
근거는 `analysis/p0_probe.py`, 산출물 `outputs/p0/theta_probe.csv`).
즉 잘린 값은 실재하며, 원리상 하한 9999.9 이상이다.

하한 검열도 있다. 순 배당률이 1.0배 미만이면 수득금을 줄여 1.0배로 고정한다.
분포가 양쪽에서 잘려 있다.

### 확정된 상수 (연구자 확인, 2026-08-11)

| 상수 | 값 | 비고 |
|---|---|---|
| 공제율 (단승·연승) | 20% | 기간 중 불변 |
| 공제율 (복승·복연승·쌍승) | 27% | 기간 중 불변 |
| 최소 베팅 단위 | 100원 | 기간 중 불변 |
| 표시 상한 θ | 9999.9 | 측정으로 확인 |
| 배당률 하한 | 1.0 | 규정 |

삼복승·삼쌍승의 공제율은 아직 확인하지 않았다.

### 식별의 뼈대

파리뮤추얼이므로

```
배당률(i) = 풀총액 × (1 − 공제율) ÷ 조합 i에 걸린 금액
```

따라서 `걸린 금액(i) = 재원 ÷ 배당률(i)`이고, 모든 조합의 금액 합은 풀 총액이다.
풀 총액은 배당표 하단에 인쇄되어 있다(`쌍승식 매출총액 …원`). 즉

```
잘린 조합들의 금액 합 = 풀총액 − 관측된 조합들의 금액 합
```

우변이 전부 관측값이라는 점이 출발점이다.

### 잔차법이 실제로는 통하지 않는다 — 이것이 이 항목의 핵심

2018-01-06 서울 2경주로 수치를 넣으면:

- 쌍승 매출총액 260,643,600원 → 재원 190,269,828원
- 12두, 조합 132개, 그중 θ 8개
- 잘린 셀 하나의 금액 상한 = 190,269,828 ÷ 9999.9 ≈ 19,027원
- 8개를 다 합쳐도 **최대 15만원 남짓**

그런데 그 경주의 최저 쌍승 배당은 5.0이고, 그 조합의 금액은 약 3,805만원이다.
배당률이 소수 첫째 자리까지만 인쇄되므로 실제 값은 5.0–5.1 구간이고,
이 폭만으로 금액이 **±19만원** 흔들린다.

**단 한 셀의 인쇄 반올림 오차가 잘린 8개 전체의 합보다 크다.**

구조적 문제다. 배당이 작은 조합은 금액이 크고 절대 오차도 크다. 반면 알고 싶은 것은
배당이 극단적으로 큰, 즉 금액이 극히 작은 조합이다. 큰 수들의 잔차로 작은 수를
잡으려는 시도이며 일반적으로 실패한다.

→ **점추정은 포기하고 부분식별로 간다.**

### 가능한 것

각 관측 셀의 금액은 구간이다.

```
금액(i) ∈ [ 재원 / (배당(i) + 0.1),  재원 / 배당(i) ]
```

(정확한 구간은 인쇄 반올림 규칙에 달려 있다. 아래 참조.)
구간 산술로 잘린 부분의 합에 대한 유효한 상하한이 나온다. 넓지만 정직하다.

셀 단위 제약은 오히려 잔차보다 강하다.

```
9999.9 ≤ 실제 배당 ≤ 재원 ÷ 100원
```

(상한은 최소 한 장은 팔렸다는 가정에서 온다. 배당이 인쇄되었으므로 성립한다.
아무도 사지 않은 조합의 처리 규칙은 별도 확인 필요 — 미적중 마번 발생 시
0.7–0.8배 적용 등 예외 규정이 있다는 2차 자료가 있다.)

### 좁힐 때 쓸 수 있는 교차 제약

같은 두 마리에 대해 서로 다른 풀이 독립적인 정보를 준다.

- **복연승** — 그 쌍에 걸린 총액을 준다. 쌍승 두 방향의 합과 관계를 갖는다
- **쌍승 역방향** — (a→b)가 잘렸어도 (b→a)가 인쇄되어 있으면 쌍의 배분에 제약이 걸린다.
  `outputs/p0/theta_probe.csv`의 `exacta_reverse` 열이 이것이다
- **삼쌍승 / 삼복승** — 같은 쌍을 포함하는 3두 조합들이 추가 제약을 준다

여러 풀에 걸친 선형 제약의 교집합을 구하는 문제가 된다.
이것이 단순 계산이 아니라 식별 문제인 이유이며, 논문 소재가 될 수 있는 이유다.

### 먼저 확정해야 할 것 — 인쇄 반올림 규칙

버림인지 반올림인지에 따라 위 구간의 끝점이 바뀐다. 경마시행규정 원문을 웹에서
찾지 못했다(2026-08-11 검색). 마사회 내부 규정으로 전문이 공개되어 있지 않다.

**측정으로 확정할 수 있으며, 그 편이 낫다.** θ가 하나도 없는 경주를 쓴다.
모든 조합의 배당률, 풀 총액, 공제율을 다 알므로 금액을 역산해 합하면 풀 총액이
복원되어야 한다. 버림 가설과 반올림 가설 중 어느 쪽이 복원하는지 대조한다.
2018년만 해도 그런 경주가 2,500건 이상이다.

부수적으로 100원 단위 제약이 추가 정보를 준다. 역산 금액이 100의 배수여야 하므로
반올림 규칙 후보를 더 걸러낼 수 있다.

### 왜 지금이 아닌가

SPEC_P0 8절이 이번 단계에서 하지 않을 것으로 "오버라운드·공제율 추정"을 명시한다.
이 항목이 정확히 그것이다.

그리고 판정 기준에 대면 분명하다 — 이 항목이 해결되지 않아도 "데이터가 맞다는 것을
어떻게 아는가"라는 P0의 주장은 무너지지 않는다. B형이다.

P2 아이디어 평가에서 다룬다. 그때 필요한 재료는 P0 산출물에 이미 들어간다
(θ 셀의 좌표, 역방향 쌍승 값, 같은 쌍의 복연승 값, 풀별 매출총액).
따로 준비할 것은 없다.
