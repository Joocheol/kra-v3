# AUTOMATION_LOG — 절차가 막힌 지점

`PROCESS.md` 0절은 **"자동화가 실패한 지점이 이 프로젝트의 일차 산출물"**이라고
선언한다. 이 파일이 그 기록이다. 선언만 있고 그릇이 없으면 기록은 대화 속에만
남고 사라진다.

한 항목은 셋을 적는다 — **무엇이 막혔나 / 사람이 무엇을 대신했나 / 고칠 수
있는 것인가.** 마지막이 중요하다. 원리적으로 못 하는 일과, 할 수 있는데 아직
안 한 일과, 설계가 틀려서 못 하는 일은 결론이 다르다.

분류:

| 등급 | 뜻 |
|---|---|
| **구조** | 현재 도구 구성으로는 원리적으로 불가. 사람이 상시 개입 |
| **설계** | 우리가 만든 것이 잘못 설계됨. 고칠 수 있으나 비용이 있음 |
| **운용** | 절차나 습관의 문제. 규칙으로 고칠 수 있음 |
| **권한** | 권한 경계 때문에 막힘 |

**등급은 바뀔 수 있다.** A-01이 그 사례다. 처음에 "구조"로 적었으나 도구를
바꾸자 사라졌다. **구조로 보이는 것이 실은 도구 선택인 경우가 있다는 것이
이 로그의 첫 교훈이다.**

---

## 요약

| ID | 날짜 | 무엇이 막혔나 | 등급 | 상태 |
|---|---|---|---|---|
| A-01 | 2026-08-11 | Claude가 Dropbox 원시 데이터를 읽지 못함 | 구조 → **도구 선택** | 해소 |
| A-02 | 2026-08-11 | Claude가 `.github/` 아래를 푸시하지 못함 | 권한 | 우회 중 |
| A-03 | 2026-08-11 | 비평 원문이 자동으로 커밋되지 않음 | 설계 | 우회 중 |
| A-04 | 2026-08-11 | 라운드 상한이 문서가 아니라 단계 태그에 걸림 | 설계 | 우회함 |
| A-05 | 2026-08-11 | Claude가 명령 전에 실행 환경 상태를 확인하지 않음 | 운용 | 원인 제거 |
| A-06 | 2026-08-11 | 레포 편집 경로가 둘로 갈라져 로컬이 원격보다 뒤처짐 | 운용 | 원인 제거 |
| A-07 | 2026-08-11 | 역할 분담 표에 있는 도구가 실제로는 쓰이지 않았음 | 운용 | 해소 |

---

## A-01 — Claude가 Dropbox 원시 데이터를 읽지 못한다 · 해소

**막힌 것.** 원시 아카이브는 `~/Dropbox/kra-analysis/data/raw_collected_v3_15w/`
아래의 `.json.gz`이고, 그 안은 EUC-KR HTML이다. 대화형 Claude가 Dropbox
커넥터를 통해 읽으면 HTML 태그가 벗겨진 채 텍스트만 돌아온다. 표 구조가
사라지므로 행·열 좌표를 쓰는 어떤 측정도 불가능하다.

**사람이 대신한 것.** 모든 측정. `analysis/p0_probe.py`는 대화형 Claude가 썼고
연구자가 로컬에서 실행했다. θ·`----`·`-`의 의미 판정(SPEC_P0 1.2), 수집 메타
표본 600, θ 역방향 집계가 전부 이 경로로 나왔다. 측정 한 번마다 왕복이 한 번씩
필요했다.

**해소.** Claude Code를 도입했다(A-07). 로컬 파일시스템을 직접 읽으므로
커넥터가 개입하지 않고, 따라서 태그가 벗겨지는 문제 자체가 발생하지 않는다.

**이 항목이 남긴 것.** 처음에 이것을 "구조" 등급으로 적었다. 틀렸다. 도구
구성을 바꾸자 사라졌으므로 **도구 선택 문제**였다. 무언가를 구조적 한계로
분류하기 전에 다른 도구로 되는지 먼저 물어야 한다.

**부수 효과 하나는 유지할 값어치가 있다.** Claude가 스크립트를 쓰고 사람이
실행하는 동안, 측정 코드가 반드시 파일로 남았다. 즉석 계산하고 결과만 옮겨
적는 것보다 재현 가능하다. Claude Code로 옮긴 뒤에도 이 습관은 규칙으로
유지한다(`CLAUDE.md` 작업 습관).

## A-02 — Claude가 `.github/` 아래를 푸시하지 못한다

**막힌 것.** GitHub App 권한 경계로 워크플로 파일 쓰기가 403이다.

**사람이 대신한 것.** `.github/workflows/*.yml`과
`.github/ai-review/validate-critique.mjs`의 모든 변경.

**고칠 수 있나.** 권한 문제이며 우리가 고를 수 있는 것이 아니다. 워크플로를
건드리는 작업이 드물어 실제 비용은 낮다. Claude Code가 로컬에서 커밋하는
경로로도 확인이 필요하다.

## A-03 — 비평 원문이 자동으로 커밋되지 않는다

**막힌 것.** CI 워크플로는 비평을 PR 코멘트로 게시하고 아티팩트로 올리지만
`review/`에 커밋하지 않는다. `gate.py`는 `review/CRITIQUE_*.json`을 읽으므로,
이 단계가 없으면 게이트가 영원히 닫혀 있고 아티팩트는 30일 후 소멸한다.
`PROCESS.md`는 `review/` 전체를 커밋한다고 적어두었으나 그 일을 하는 주체가
없었다.

**사람이 대신한 것.** `scripts/fetch_critique.sh`를 로컬 실행. 라운드가 여럿이면
아티팩트 이름이 겹치므로 실행 ID를 직접 지정해야 한다 — r1 `31437393494`,
r2 `31439814297`.

**고칠 수 있나.** 워크플로가 아티팩트를 올린 뒤 같은 잡에서 커밋하도록 바꾸면
된다. A-02 때문에 사람이 커밋해야 하는 변경이다.

**주의할 점.** 손으로 옮겨 적으면 안 된다. 그것은 검증되지 않은 새 객체이며,
비평자가 실제로 무엇을 썼는지에 대한 기록을 잃는다.

## A-04 — 라운드 상한이 문서가 아니라 단계 태그에 걸린다

**막힌 것.** `critique.py`의 `MAX_ROUNDS`가 단계 태그 단위로 적용된다. P0에서
`SPEC_P0.md` 검토에 r1·r2를 모두 쓰면, 정작 P0의 산출물인 `REPORT_P0.md`를
비평할 라운드가 남지 않는다. 태그는 `phase == "M"`일 때만 `--milestone`으로
갈라지므로 P0에서는 태그를 나눌 수도 없다.

**사람이 대신한 것.** 없음. 대화형 Claude가 3라운드를 제안했다가 `critique.py`를
읽고 불가능함을 확인했다(A-05 참조).

**고칠 수 있나.** 옳은 수정은 상한을 **입력 문서 집합** 단위로 옮기는 것이며
`critique.py`와 `gate.py`를 함께 바꿔야 한다. 지금은 `MAX_ROUNDS`를 2에서 4로
올려 우회했다(커밋 `b0a3b5b`). r1–r2는 명세서, r3–r4는 리포트에 배정한다.

**대가.** r1·r2와 r3·r4는 검토 대상 문서가 다르므로 라운드 번호로 직접 비교할
수 없다. `OPEN_ITEMS.md` 프로세스 변경 기록에 적었다.

## A-05 — Claude가 명령 전에 실행 환경 상태를 확인하지 않았다 · 원인 제거

**막힌 것.** 2026-08-11 하루에 네 번. (1) `critique.py`를 읽지 않은 채 3라운드
요청서 작성을 제안했다 — `MAX_ROUNDS=2`가 그 파일에 있었다. (2) 로컬 패치
명령에 `git pull` 선행 확인을 넣지 않아, 직전에 대화형 Claude가 원격에 푸시한
`OPEN_ITEMS.md`의 옛 판본 위에 패치가 얹혔다. (3) 문자 수를 바이트로 출력했다.
(4) 복구 명령에서 `OPEN_ITEMS.md`만 되돌렸는데, **직전 출력에 `PROCESS.md`도
수정됐다고 찍혀 있었다.** `git pull`이 거부됐다.

**사람이 대신한 것.** 연구자가 오류 빈도를 지적했다. (2)는 대화형 Claude가
출력의 문자 수 이상을 보고 스스로 잡았으나 우연이었다.

**공통 원인.** Claude가 **사고 대상은 확인하고 실행 환경 상태는 확인하지
않았다.** 네 건 모두 필요한 정보가 이미 화면이나 레포에 있었다.

**원인 제거.** 넷 중 셋이 "로컬 상태를 볼 수 없어 추측했다"에서 왔다. Claude
Code는 로컬을 직접 보므로 추측할 필요가 없다. 남는 것은 (1)과 (3) 같은
"확인할 수 있었는데 안 했다"이며, 이것은 규칙으로만 줄인다 — 명령이나 변경
전에 전제를 나열하고 확인 출처를 적는다(`CLAUDE.md`).

**관찰 대상.** 이 등급이 실제로 줄어드는지가 메타 목적의 지표다.

## A-06 — 레포 편집 경로가 둘로 갈라졌다 · 원인 제거

**막힌 것.** 대화형 Claude는 GitHub API로 푸시하고 연구자는 로컬에서 패치하는
구조에서, 연구자가 `git pull`을 하지 않으면 원격보다 뒤처진 판본에 패치가
얹힌다. 그대로 커밋하면 Claude가 푸시한 내용이 조용히 사라진다. A-05의 (2)와
(4)가 이 구조에서 나왔고, 로컬이 인계 시점보다 두 커밋 뒤처져 있었다는 사실도
이때 드러났다.

**사람이 대신한 것.** 되돌리고 다시 받고 다시 붙이는 복구를 두 번.

**원인 제거.** 레포에 쓰는 손을 하나로 만들었다. Claude Code가 로컬에서 직접
읽고 쓰고 커밋하므로 원격/로컬 격차가 생기지 않는다. 대화형 Claude는 읽기만
한다.

**남는 위험.** 양쪽을 같은 시간대에 쓰면 재발하며, 이번에는 사람이 알아채기
더 어렵다 — 양쪽 다 자기가 최신이라고 믿는다. `PROCESS.md` 분업 절에 "예외를
두려면 그 세션 동안 Claude Code를 쓰지 않는다는 것을 먼저 확인한다"를 넣었다.
이 규칙은 강제되지 않고 습관에 의존한다.

## A-07 — 역할 분담 표에 있는 도구가 실제로는 쓰이지 않았다 · 해소

**막힌 것.** `PROCESS.md` 역할 분담 표에 **"분석 | Claude Code | 코드, 데이터,
리포트 생성"**이 처음부터 적혀 있었으나, 2026-08-11 하루 종일 Claude Code는 한
번도 쓰이지 않았다. 코드는 대화형 Claude가 쓰고 연구자가 실행했다. 그 상태에서
분업 규칙까지 "대화형 Claude가 API로 푸시한다"를 전제로 새로 얹혔다. 문서와
현실이 어긋난 채 그 위에 규칙이 쌓였다.

**어떻게 드러났나.** 연구자가 "우리 프로세스에는 클로드 코드 사용이 없는 것
같다"고 지적했다. 모델 쪽에서 발견하지 못했다 — 문서에 적혀 있으니 있는 것으로
읽었다.

**파급.** A-01을 "구조" 등급으로 잘못 분류한 원인이 이것이다. 쓸 수 있는 도구가
있는데 안 쓴 상태를 도구의 한계로 적었다.

**해소.** Claude Code를 실제 작업 주체로 세우고, `CLAUDE.md`를 만들어 이
프로세스를 세션마다 읽게 했다(커밋 `4935294`). `PROCESS.md`의 역할 분담과 분업
절을 현실에 맞게 다시 썼다(`5830d6a`).

**교훈.** 문서가 선언한 구성과 실제 구성이 같은지 주기적으로 대조해야 한다.
선언만 있고 실행되지 않는 항목은 **없는 것보다 나쁘다** — 있는 것으로 믿고 그
위에 다른 결정을 쌓게 된다.

---

## 이 기록을 어떻게 쓸 것인가

논문에 그대로 들어가지는 않는다. 그러나 **"연구 절차를 어디까지 자동화할 수
있는가"라는 메타 목적의 유일한 증거**가 이 파일이다. 등급별로 결론이 다르다.

- **구조**가 많으면 자동화의 천장이 낮다는 뜻이다
- **설계**가 많으면 우리가 만든 것이 미숙하다는 뜻이고, 고칠 수 있다
- **운용**이 많으면 규칙으로 줄일 수 있다. 줄어드는지가 관찰 대상이다

첫 집계에서 구조 1·설계 2·운용 2·권한 1이었다. 도구 구성을 바꾸자 구조 0·설계
2·운용 3·권한 1이 됐다. **구조 등급이 0이 된 것이 오늘의 가장 큰 변화이며,
동시에 가장 조심할 대목이다** — 하루치 표본이고, 아직 대규모 실행을 한 번도
해보지 않았다. 어떤 경향도 읽지 않는다.
