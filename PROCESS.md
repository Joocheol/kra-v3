# PROCESS — 비평 게이트가 걸린 연구 진행 절차

## 0. 이 프로젝트가 무엇인가

**지금 논문을 쓰고 있는 것이 아니다.** 목적이 두 층이다.

| 층 | 무엇을 하는가 |
|---|---|
| 목적 | KRA 배당 데이터에서 **논문이 될 아이디어를 찾는다** |
| 메타 목적 | 그 과정이 **어느 정도까지 자동화되는지 시험한다** |

이 문서의 나머지는 논문을 쓰는 절차처럼 쓰여 있다. 그것은 **연습이 아니라
시험이기 때문이다** — 절차를 진짜로 돌려보지 않으면 그것이 작동하는지 알 수
없다. 하지만 절차를 돌리는 것과 지금 논문을 쓰고 있는 것을 혼동하면 안 된다.

### 이 구분이 실제로 바꾸는 것

**범위 통제는 지금 지킬 규율이 아니라 지킬 수 있는지 시험할 대상이다.**
탐색 중에 나온 아이디어를 "범위 밖"이라며 무조건 백로그로 보내면, 시험할
대상이 사라진다. 대신 `IDEAS.md`에 적고 P2에서 물리게 한다.

다만 **게이트 순서는 그대로 지킨다.** 한 단계가 닫히기 전에 다음 것을 시작하면
그순간 게이트를 우회한 첫 사례가 된다. v2가 무너진 방식이 정확히 그것이며,
시험 중에 시험 대상을 부수는 셈이다.

**산출물은 두 종류다.** 하나는 연구 결과이고, 다른 하나는 **이 절차가 어디서
막혔는가에 대한 기록**이다. 후자를 버리지 않는다. 자동화가 실패한 지점 —
사람이 개입해야만 했던 곳, 비평이 헛짚은 곳, 게이트가 잘못 닫힌 곳 — 이 이
프로젝트의 일차 산출물이다.

**그 기록은 `AUTOMATION_LOG.md`에 쌓는다.** 선언만 있고 그릇이 없으면 기록은
대화 속에만 남고 사라진다. 막힌 지점이 나올 때마다 그 자리에서 등재한다.
나중에 회상해서 쓰면 이미 절반은 잃은 뒤다.

**규칙 자체가 시험 대상이다.** 이 문서는 확정된 규정집이 아니라 시험 중인
가설이다. 작동하지 않는 규칙은 고친다. 다만 고칠 때는 **왜 고치는지와 무엇을
포기하는지**를 함께 적는다. 조용히 완화하는 것이 v2가 무너진 방식이다.

---

## 왜 이 구조인가

사람이 논문을 쓸 때는 매 단계 끝에 세미나에서 발표하고, 깨지고, 반박하거나
고치거나 포기하고, 그 결정에 따라 다음으로 간다. 이 문서는 그 루프를 파일과
게이트로 구현한 것이다.

이전 프로젝트(v2)에서 비싼 실패는 엄밀성 부족이 아니었다. 검증 장치는 오히려
과할 정도였다. 무너진 것은 **범위 통제와 정지 규칙**이었다 — 분석에 종료 조건이
없었고, 원고를 분석 동결 전에 썼다. 이 프로세스는 그 두 가지를 게이트로 강제한다.

## 역할 분담

| 역할 | 담당 | 하는 일 |
|---|---|---|
| 분석 | **Claude Code** | 코드, 데이터, 리포트 생성. 레포 커밋 |
| 설계 보조 | 대화형 Claude (웹) | 판정 초안, 비평 해석, 프로세스 설계. **파일은 건드리지 않는다** |
| 비평 | ChatGPT (자동) | 문서만 읽고 공격 |
| **판정** | **연구자** | 반박·수정·포기·백로그 결정 |

**판정을 모델에게 넘기지 않는다.** 두 모델이 합의해서 함께 틀리는 것이 가장
나쁜 결말이다. 게이트는 판정이 존재하는지만 확인하고, 판정이 옳은지는 묻지
않는다.

## 분업 — 누가 어느 파일을 건드리는가

역할 분담과 다른 층이다. 위는 **무엇을 결정하는가**이고, 이것은 **누구의 손이
파일에 닿는가**이다.

| 주체 | 손이 닿는 곳 |
|---|---|
| Claude Code | 레포 전체. 로컬에서 읽고 쓴다. **작업 브랜치에만** 커밋·푸시한다 |
| 연구자 | `review/RESPONSE_*.md` 판정, `.github/` 커밋, `p0-spec` 병합 |
| 대화형 Claude | 없음. 읽기만 한다 |

**한 시점에 레포에 쓰는 주체는 하나여야 한다.** 2026-08-11에 대화형 Claude가
GitHub API로 푸시하고 연구자가 로컬에서 편집하는 구조로 일했고, 로컬이 원격보다
뒤처진 채 패치가 얹히는 사고가 두 번 났다(`AUTOMATION_LOG.md` A-06). Claude
Code는 로컬에서 직접 읽고 쓰므로 그 격차 자체가 생기지 않는다.

**대화형 Claude는 파일을 바꾸지 않는다.** 판정 초안·설계 논의·비평 해석에
쓴다. 변경이 필요하면 Claude Code에 넘긴다. 예외를 두려면 **그 세션 동안 Claude
Code를 쓰지 않는다는 것을 먼저 확인한다.** 확인 없이 양쪽을 쓰면 A-06이
재발하며, 이번에는 사람이 알아채기 더 어렵다 — 양쪽 다 자기가 최신이라고 믿는다.

**쓰는 손이 하나여도 브랜치는 나눈다.** Claude Code는 작업 브랜치에 스스로
커밋·푸시하고, `p0-spec` 병합은 연구자가 지시할 때만 **squash**로 한다. 이유는
분업이 아니라 기록이다 — 명세서가 요구하는 "실행보다 먼저 커밋"의 유일한 증거가
타임스탬프인데, `p0-spec`에 "같은 산출물을 두 번 만들었다"가 남으면 결과를 보고
다시 만든 것 아니냐는 의심을 부른다. 작업 브랜치는 시행착오가 남아도 되고,
squash 병합이면 `p0-spec`에는 확정 시점 하나만 남는다.

**작업 시작 전에 `git pull`.** 로컬 HEAD가 `origin/p0-spec`보다 뒤처져 있으면
시작하지 않는다. 이 규칙이 없어서 2026-08-11에 `CLAUDE.md`가 양쪽에서 따로
만들어졌다(A-08). 쓰는 손을 하나로 만든 것만으로는 A-06이 닫히지 않았다.

**Claude Code가 지킬 규칙은 `CLAUDE.md`에 있다.** 세션마다 자동으로 읽힌다.
프로세스를 바꿀 때 `PROCESS.md`만 고치면 Claude Code는 옛 규칙을 따른다.
**두 파일을 같은 커밋에서 고친다.**

**예외.** `.github/` 아래는 권한 밖이다(A-02). 연구자가 커밋한다.

## 단계

| 단계 | 산출물 | 비평의 초점 |
|---|---|---|
| **P0** 데이터 검증 | `REPORT_P0.md` | 파생 필드, 순환 검정, 결측 구조, 특수값 |
| **P1** 탐색 | `FACTS.md`, `EXPLORATION_LOG.md` | **"이건 데이터 결함 아닌가"** |
| **P2** 아이디어 평가 | `IDEAS.md` | 최강 반론, 양방향 논문성, 식별 |
| **P3** 사전분석계획 | `PAP.md`, `CLAIMS.md` | 잔여 자유도, 무근거 상수, 기각 조건 |
| **M1..** 확증 분석 | `SPEC_M*`, `REPORT_M*` | PAP 준수, 실패 불가능 게이트, 범위 신호 |
| — 집필 | | (동결 이후에만) |

각 단계는 `make p1` 처럼 실행하며, **이전 단계의 게이트가 열려 있지 않으면
실행 자체가 막힌다.**

## 루프

```
분석 실행
   ↓
critique.py  →  review/CRITIQUE_<TAG>_r1.md   (판정 의무 항목 최대 7개)
   ↓
연구자가    →  review/RESPONSE_<TAG>_r1.md    (항목별 판정 + 근거)
   ↓
gate.py     →  전부 판정됐는가?  아니면 exit 1
   ↓
(선택) 다음 라운드 — 직전 라운드 응답을 첨부하고 재제기 금지
   ↓
다음 단계 열림
```

## 판정 네 가지

| 판정 | 의미 | 근거에 반드시 포함 |
|---|---|---|
| **반박** | 비평이 틀렸다 | 틀렸음을 보이는 파일·행·수치·절 번호 |
| **수정** | 맞다, 고쳤다 | 커밋 해시 |
| **포기** | 맞다, 해당 주장을 버린다 | 무엇을 버렸고 무엇이 남았는지 |
| **백로그** | 맞지만 현재 주장을 위협하지 않는다 | `BACKLOG.md` 항목 번호 |

**반박이 가장 많이 나올 것이다.** 외부 비평자는 맥락이 없으니 절반은 이미
처리된 것을 지적한다. 그래도 반박을 기록으로 남기는 것이 이 구조의 핵심
가치다. 나중에 리뷰어가 같은 지적을 했을 때 답이 이미 준비되어 있다.

**수정을 기록할 때는 "무엇을 추가했다"가 아니라 "이 수정 이후 무엇을 할 수
없게 되었는가"를 쓴다.** 자유도가 줄지 않았으면 수정이 아니다. P0 r1의 수정
일곱 개 중 셋이 자유도를 없앤 것이 아니라 옮긴 것이었고, r2가 그것을 전부
찾아냈다.

## 노이즈 억제 (없으면 사흘 만에 무너진다)

자동으로 도는 비평자는 반드시 헛소리를 대량 생산한다. 네 가지 장치가 걸려 있다.

1. **판정 의무 항목 7개 상한.** 초과분은 `A` 항목(참고)으로 강등되며 게이트에
   영향을 주지 않는다.
2. **파급 진술 의무.** 각 항목은 "이것이 사실이면 무엇이 무너지는가"를 반드시
   포함해야 한다. 30자 미만이면 `critique.py`가 자동으로 참고 항목으로 강등한다.
   이 강등은 모델의 자기 신고와 무관하게 일방향으로만 작동한다.
3. **라운드 상한 4.** 각 라운드 프롬프트에는 직전 라운드 응답이 첨부되고
   재제기가 금지된다. 상한에 도달한 뒤에도 남으면 연구자가 판정하고
   `OPEN_ITEMS.md`에 적고 진행한다.

   **상한은 단계 태그에 걸린다.** 같은 태그에서 명세서와 리포트를 모두 비평하면
   라운드를 나눠 써야 한다. P0가 그 경우이며, r1–r2는 `SPEC_P0.md`,
   r3–r4는 `REPORT_P0.md`에 배정한다. 상한이 문서 집합이 아니라 태그에 걸려
   있다는 것은 설계 결함이고, 고치려면 `critique.py`와 `gate.py`를 함께
   바꿔야 한다. 지금은 상한을 올리는 것으로 우회한다(`AUTOMATION_LOG.md` A-04).
4. **비평자는 코드와 원시 데이터를 보지 않는다.** 문서만 본다. 문서에 없는
   숫자를 만들면 그 항목은 무효다.

## 범위 통제 — A형 / B형

M 단계 비평은 매번 "이 문서가 새로 연 질문"을 목록으로 내놓는다. 각각에 대해
질문은 하나다.

> **이 항목이 해결되지 않으면 `CLAIMS.md`의 어느 ID가 거짓이 되는가?**

- 특정 ID를 지목할 수 있으면 **A형(검증 부채)** — 지금 처리한다. 협상 불가.
- 지목할 수 없으면 **B형(범위 확장)**.

**B형의 행선지는 단계에 따라 다르다.** 0절을 보라.

- 집필 이후 → `BACKLOG.md`. 예외 없음. 다음 논문이라는 뜻이다
- 아이디어 탐색 중(현재) → 연구 질문이라면 `IDEAS.md`, 부수 작업이라면 `BACKLOG.md`

어느 쪽이든 **지금 실행하지는 않는다.** 적어두고 현재 단계를 닫는다.
v2에서 원고 완성 이후에 들어온 신규 분석 단계는 전부 B형이었고, 그것이 원고를
반복해서 무효화했다.

## 동결

집필 착수 전에 세 조건이 모두 0이어야 한다.

- 근거 없는 상수: **0개**
- 실패 불가능한 게이트: **0개**
- 미분류 미결 항목: **0개**

동결 이후 **B형 항목의 진입 자체가 금지**된다. `BACKLOG.md`에 적고 넘어간다.
복제 패키지 릴리스는 투고와 **동시에** 한다. 먼저 하지 않는다.

## 탐색과 확증의 분리

P1은 탐색이고, 거기서 나온 가설은 탐색적으로 발견된 것이다. `PAP.md`에 그
사실을 명시하고, 논문에서도 그렇게 쓴다. 처음부터 예측했던 것처럼 쓰면 그건
HARKing이며, 이 프로세스에서 가장 조심해야 할 지점이다.

`EXPLORATION_LOG.md`는 시도한 모든 것을 채택·기각 불문하고 기록한다. 두 가지
역할을 한다: "왜 이건 안 봤나"에 대한 답, 그리고 forking paths 방어.

프라이빗 레포에서도 커밋 타임스탬프는 유효한 증거다. P3 사전분석계획이 확증
분석 착수 **전에** 커밋되었다는 사실이 순서를 증명한다. 같은 이유로, 명세서가
"실행 전에 커밋"을 요구하는 항목들은 **순서 자체가 근거다**. 사후에 커밋해서
순서를 맞춘 것처럼 보이게 하는 것이 최악이다.

## 비평자를 어디서 돌리는가

두 가지 경로가 있고, **CI가 기본**이다.

| 경로 | 트리거 | 쓸 때 |
|---|---|---|
| **CI (기본)** | PR에 `gpt-critique` 라벨 | 마일스톤, 코드가 딸린 모든 단계 |
| 로컬 | `make critique-P1` | P0~P2의 문서만 있는 초기 탐색 |

CI 경로가 기본인 이유는 v1의 `ai-review` 워크플로에서 검증된 성질들 때문이다.

- **PR head SHA 고정.** 라벨 이벤트 이후 head가 바뀌면 실패한다. 비평이 어느
  커밋을 봤는지가 확정된다.
- **결정론적 검증.** `.github/ai-review/validate-critique.mjs`가 파생 필드를
  전부 재계산한다. 비평자는 자기가 얼마나 많은 일을 만들지 스스로 정하지 못한다.
  강등은 일방향이다.
- **비평자에게 도구가 없다.** 문서만 읽는다. 레포·diff·PR 내용은 전부 신뢰할 수
  없는 증거이지 지시가 아니다.
- **사람이 깨어 있을 필요가 없다.** 라벨을 붙이면 결과가 PR 코멘트로 온다.

### 알려진 구멍 — 비평 원문이 자동으로 커밋되지 않는다

워크플로는 비평을 PR 코멘트로 게시하고 아티팩트로 올리지만, **아무도 그것을
커밋하지 않는다.** `gate.py`는 `review/CRITIQUE_*.json`을 읽으므로 이 단계가
없으면 게이트가 영원히 닫혀 있고, 기록은 30일 후 소멸한다.

`scripts/fetch_critique.sh`를 돌려 받아온다. 라운드가 여럿이면 아티팩트 이름이
같으므로(`gpt-critique-<PR번호>`) 실행 ID를 직접 지정해야 한다.

`AUTOMATION_LOG.md` A-03에 등재되어 있다.

## REVIEW_REQUEST.md — 정적 프롬프트보다 중요하다

`prompts/critique_*.md`는 단계별 고정 질문이다. 그러나 비평의 품질을 실제로
결정하는 것은 **매 라운드 갱신하는 `REVIEW_REQUEST.md`**다. v1의
`CLAUDE_REVIEW_REQUEST.md`와 같은 역할이다.

네 부분으로 이루어진다.

1. **상태** — 비평자는 이 프로젝트의 역사를 모른다.
2. **이번 라운드의 범위** — 범위 밖은 명시적으로 배제한다.
3. **이미 닫힌 것** — 가장 중요하다. 비어 있으면 비평자는 매 라운드 같은 근본
   질문을 다시 꺼내고, 저자는 매 라운드 같은 반박을 쓴다.
4. **이번에 확인할 것** — 번호 붙인 구체적 질문 5~12개.

헤더의 yaml 블록이 `phase`/`tag`/`round`/`inputs`를 지정하고, `critique.py
--from-request`가 그것을 읽는다. **문서 하나가 이번 라운드의 전부를 정의한다.**

**검토 대상 문서가 실재한 뒤에 쓴다.** 아직 만들지 않은 산출물을 상상해서
질문을 적으면, 비평자가 그 상상에 대해 답한다.

## requires_author_decision

각 비평 항목은 이 플래그를 갖는다.

- `false` — 채택된 범위 **안에서** 고칠 수 있다. 구현 결함, 누락된 검정,
  근거 없는 상수.
- `true` — 연구범위·식별가정·주장 자체를 바꾸어야 한다. 저자만 결정할 수 있다.

앞서 말한 A형/B형과 층이 다르다. A/B형은 **언제 처리할지**(지금 vs 백로그)를
가르고, 이 플래그는 **누가 결정하는지**를 가른다. 둘 다 필요하다.

## 설정

```bash
export OPENAI_API_KEY=...
export OPENAI_MODEL=...      # 반드시 명시. 기본값 없음.
pip install -r requirements.txt
```

CI에서는 `OPENAI_API_KEY`를 repository secret으로, `OPENAI_MODEL`을 repository
variable로 둔다.

`OPENAI_MODEL`에 기본값을 두지 않는 것은 의도적이다. 어느 모델이 비평했는지는
기록의 일부이며, `CRITIQUE_*.json`에 남는다. 프로젝트 중간에 모델을 바꾸면
비평의 성격이 달라지므로, 바꿀 때는 `OPEN_ITEMS.md`에 적는다.

Claude Code는 레포 루트에서 실행한다. `CLAUDE.md`가 자동으로 읽히며,
`/memory`로 어느 지시 파일이 활성인지 확인할 수 있다. 공식 문서:
https://docs.claude.com/en/docs/claude-code/overview

## 파일 목록

```
PROCESS.md              이 문서
CLAUDE.md               Claude Code가 세션마다 읽는 규칙 (PROCESS와 같이 고친다)
REVIEW_REQUEST.md       현재 라운드의 범위 지정 (매 라운드 갱신)
CONVENTIONS.md          v2에서 검증된 분석 규약
CLAIMS.md               주장 대장 (해석 천장 포함)
FROZEN.txt              동결 산출물 목록 + 허용오차 등급
FACTS.md                P1 이 확정한 사실
IDEAS.md                P2 평가 대상 — 논문 후보와 그 죽는 조건
BACKLOG.md              B형 부수 작업 적재소 (연구 질문은 IDEAS.md 로)
OPEN_ITEMS.md           라운드 상한 후에도 남은 것, 미해결 채로 진행하는 것
EXPLORATION_LOG.md      P1의 전수 기록
AUTOMATION_LOG.md       절차가 막힌 지점 — 메타 목적의 일차 산출물 (0절)

prompts/_common.md      모든 비평에 주입되는 공통 규칙
prompts/critique_*.md   단계별 비평 질문
scripts/critique.py     비평 호출 + 규칙 강제
scripts/gate.py         응답 완결성 검사
scripts/fetch_critique.sh  CI 아티팩트에서 비평 원문 회수
scripts/check_process.py  프로세스 위생 (매 PR)
scripts/check_freeze.py   동결 계약 검증 (매 PR)
templates/              응답 + REVIEW_REQUEST 템플릿
.github/workflows/analysis-ci.yml        위생 + 동결 (자동)
.github/workflows/gpt-critique.yml       라벨 트리거 비평
.github/ai-review/validate-critique.mjs  결정론적 출력 검증
review/                 CRITIQUE / RESPONSE / PROMPT 기록 (전부 커밋)
```

`review/` 전체를 커밋한다. 프롬프트도 커밋한다. 어떤 질문을 던졌는지가
어떤 답을 받았는지만큼 중요하다.
